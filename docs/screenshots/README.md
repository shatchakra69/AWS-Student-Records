# Screenshots

Drop deployment screenshots here after your first `terraform apply`, then link
them from the main README. Good ones to capture:

- The running student-records website (via the ALB DNS URL)
- EC2 → Target Groups showing 2 **healthy** targets
- EC2 instances spread across two Availability Zones
- RDS instance (Single-AZ, no public access)
- CloudWatch alarms in OK state
